# Global gitignore — honored by git, fd (telescope find_files), and rg (telescope live_grep).
# Applies inside any git repo. Dotfiles like .docs_claude are NOT listed here, so they
# still show in telescope (with find_files hidden=true); only the junk below is hidden.

# ML experiment logging
wandb/

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/

**/.claude/settings.local.json
